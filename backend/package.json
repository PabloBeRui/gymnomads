{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend server for the GymNomads project",
  "main": "index.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test jest --testTimeout=10000 --coverage",
    "test:watch": "cross-env NODE_ENV=test jest --watch",
    "test:coverage": "cross-env NODE_ENV=test jest --coverage",
    "db:test:create": "mysql -u root -p < ../bbdd/create_test_db.sql",
    "dev": "nodemon index.js",
    "test:connection": "node scripts/db-connection-test.js"
  },
  "keywords": [],
  "author": "Pablo Bellon",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "mysql2": "^3.15.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  }
}
