# Environment variables for the GymNomads Backend
# Copy this file to .env and modify the values if necessary

# Variables de entorno para el Backend de GymNomads
# Copia este archivo a .env y modifica los valores si es necesario

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=gymnomads

# Secreto para firmar los JSON Web Tokens (JWT)
# Secret for signing JSON Web Tokens (JWT)
JWT_SECRET=